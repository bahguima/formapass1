<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fp">

    <application
        android:allowBackup="true"
        android:icon="@drawable/fp"
        android:label="FormaPass"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".listaConv"/>
        <activity android:name=".qrcodeevento2" />
        <activity android:name=".qrcodeevento1" />
        <activity android:name=".qrcodeevento" />
        <activity android:name=".telaAjuda" />
        <activity android:name=".mainevento" />
        <activity android:name=".digiteevento" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>